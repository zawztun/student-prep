/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    serverComponentsExternalPackages: ['@prisma/client'],
  },
  images: {
    domains: ['flagcdn.com'], // For country flag images
  },
};

module.exports = nextConfig;